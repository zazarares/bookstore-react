import React from 'react';
import '../Styles/loading.css'; // Optional: For adding custom styles

const LoadingSpinner = () => {
    return (
        <div className="spinner-container">
            <div className="loading-spinner">
            </div>
        </div>
    );
};

export default LoadingSpinner;